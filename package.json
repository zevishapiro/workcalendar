{
  "name": "workcalendar",
  "version": "1.0.0",
  "private": true,
  "description": "Hours: a one-field work hours tracker.",
  "scripts": {
    "test": "node parser.test.js && node stats.test.js",
    "start": "npx serve ."
  }
}
